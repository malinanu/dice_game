{
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "start": "concurrently \"cd client && npm install && npx vite\" \"npm install && npm  start --prefix Server\""
  }
  
  
  
  
  
}
