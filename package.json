{
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "scripts": {
    "start": "concurrently \"cd client && npm install && npx vite build && npx vite preview\" \"npm install && npm start --prefix Server\""
  }
  
  
  
  
  
}
